import Navbar from "@/components/Navbar";

export default function Home() {
  return (
    <div className="min-h-screen bg-white">
      <Navbar />
      
      {/* Other sections will be added here as we build them */}
      {/* Example:
      <HeroSection />
      <ServicesSection />
      <PricingSection />
      etc.
      */}
    </div>
  );
}